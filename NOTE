There is some latency when querying for the directory size which makes it seem like the program is not working.It is in fact working.  Its just the program takes a while to return from SRBFile.length().  Once it returns from that, you will see the progress bar moving indicating a transfer is occurring.

Specify from the JNLP where to download all required jar files that the application needs.
